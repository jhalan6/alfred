learn:
	zip -j learn_and_think.alfredworkflow ./learn_think/src/*

of:
	zip -j omnifocus.alfredworkflow ./omnifocus/src/*

qof:
	zip -j qof.alfredworkflow ./quick_import_of/src/*

clean:
	rm *.alfredworkflow
